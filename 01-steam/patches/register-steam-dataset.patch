diff --git a/src/calc2/store/groups.ts b/src/calc2/store/groups.ts
--- a/src/calc2/store/groups.ts
+++ b/src/calc2/store/groups.ts
@@ -260,3 +260,10 @@ export function loadStaticGroups() {
 	}[] = [
+			{
+				maintainerGroup: 'FIUBA',
+				maintainer: '',
+
+				source: 'local',
+				id: 'steam',
+			},
 			{
 				maintainerGroup: t('calc.maintainer-groups.misc'),
diff --git a/src/calc2/utils/groupUtils.ts b/src/calc2/utils/groupUtils.ts
--- a/src/calc2/utils/groupUtils.ts
+++ b/src/calc2/utils/groupUtils.ts
@@ -10,11 +10,13 @@ import * as jQuery from 'jquery';
 import {string} from "prop-types";
 
 const ld_sb: any = require('../data/sb.txt');
+const ld_steam: any = require('../data/steam.txt');
 const ld_ufes: any = require('../data/ufes.txt');
 const ld: any = require('../data/misc.txt');
 const ld_uibk: any = require('../data/uibk.txt');
 const LOCAL_DATA: { [id: string]: string } = {
 	'sb': ld_sb.default ? ld_sb.default : '',
+  'steam': ld_steam.default ? ld_steam.default : '',
   'ufes': ld_ufes.default ? ld_ufes.default : '',
   'misc': ld.default ? ld.default : '',
   'uibk': ld_uibk.default ? ld_uibk.default : '',
diff --git a/src/calc2/main.tsx b/src/calc2/main.tsx
--- a/src/calc2/main.tsx
+++ b/src/calc2/main.tsx
@@ -64,6 +64,6 @@ export class Main extends React.Component<Props, State> {
 					<I18NProvider>
 							<Switch>
-								<Redirect exact from="/" to={`/relax/landing`} />
+								<Route exact path="/" component={ConnectedCalc} />
 								<Redirect exact from="/relax" to={`/relax/landing`} />
 								<Route path="/relax/landing" component={Landing} />
 								<Route path="/relax/help" component={Help} />
diff --git a/src/calc2/views/calc.tsx b/src/calc2/views/calc.tsx
--- a/src/calc2/views/calc.tsx
+++ b/src/calc2/views/calc.tsx
@@ -80,7 +80,7 @@ export class Calc extends React.Component<Props> {
 	}
 
 	private loadGroup(props: Props) {
-		const { source, id, filename = '', index = '0' } = props.match.params;
+		const { source = 'local', id = 'steam', filename = 'local', index = '0' } = props.match.params;
 		const requestId = `${source}/${id}/${filename}/${index}`;
 
		// mark that we triggered a load explicitly
diff --git a/webpack.config.js b/webpack.config.js
--- a/webpack.config.js
+++ b/webpack.config.js
@@ -31,9 +31,6 @@ module.exports = (env, options) => {
 			'calc': [
 				'./src/calc2/calculator.entry.tsx',
 			],
-			'tests': [
-				'./src/db/tests/tests.entry.ts',
-			],
 		},
 		output: {
 			path: __dirname + "/dist/",
@@ -54,13 +51,6 @@ module.exports = (env, options) => {
 				template: 'src/calc2/index.ejs',
 				excludeAssets: [/test.*.(js|css)/],
 			}),
-			// generate test.html
-			new HtmlWebpackPlugin({
-				filename: 'test.html',
-				template: 'src/db/tests/test.ejs',
-				excludeAssets: [/calc.*.(js|css)/],
-			}),
-
 			new HtmlWebpackExcludeAssetsPlugin(),
 
 			new MiniCssExtractPlugin({
@@ -82,19 +72,6 @@ module.exports = (env, options) => {
 				'NODE_ENV': isProduction ? 'production' : 'development',
 			}),
 
-			// https://github.com/webpack-contrib/zopfli-webpack-plugin
-			(isProduction
-				? new CompressionPlugin({
-					test: /\.*/,
-					algorithm: 'gzip',
-					threshold: 0,
-					minRatio: 0.8,
-					cache: true,
-					deleteOriginalAssets: false,
-				})
-				: null
-			),
-
 			// https://github.com/kevlened/copy-webpack-plugin
 			new CopyWebpackPlugin([
 				{ from: 'src/calc2/manifest.json', to: './' },
@@ -102,13 +79,6 @@ module.exports = (env, options) => {
 				{ from: 'resources/logos/*', to: './assets/logos/', flatten: true },
 			]),
 
-			new BundleAnalyzerPlugin({
-				analyzerMode: 'static',
-				reportFilename: '../reports/report.html',
-				defaultSizes: 'parsed',
-				openAnalyzer: false,
-				logLevel: 'info',
-			}),
 		].filter(e => !!e),
 		resolve: {
			extensions: ['.webpack.js', '.web.js', '.ts', '.tsx', '.js', '.html'],
@@ -169,6 +139,7 @@ module.exports = (env, options) => {
 							loader: 'ts-loader',
 							options: {
 								onlyCompileBundledFiles: true,
+								transpileOnly: true,
 							}
 						},
 					],
