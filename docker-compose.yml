services:
  relax:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        RELAX_REF: development
    image: local/relax-steam-college-mode:latest
    container_name: relax-steam-college
    ports:
      - "8088:80"
    restart: unless-stopped
